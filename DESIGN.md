# 设计约定

- **任务模式**：本地即时计算工作台。围绕四项输入比较追加资金的手续费效率，首屏直接呈现核心 APR；账本与计算口径默认折叠。
- **桌面布局**：最大宽度 1240px，左侧参数、右侧结果的双栏结构。结果依次为大号 APR、解释句、辅助指标与曲线，使用留白和细分隔线组织层级。
- **排版与颜色**：中文系统无衬线字体，数字采用等宽数字。浅灰背景 `#f5f7fa`、白色面板、深灰正文；蓝色 `#2859d9` 表示交互，深蓝突出结果。面板使用 14px 圆角与浅色边框。
- **控件**：数值输入保留独立单位，追加金额支持精确输入与滑块调整。字段说明紧邻输入；聚焦状态使用蓝色边框和清晰描边。
- **错误与零额状态**：无效字段显示红色边框与文字提示，结果区切换为待补全状态，避免保留旧结果。追加 0 U 不呈现整笔新增年化；曲线零点解释为小额加仓极限。
- **图表语义**：曲线表示从原仓位出发，追加有限金额后，整笔新增资金的平均边际手续费 APR；“再加一小笔”单独展示加仓后的瞬时边际。结果附近持续说明手续费单利口径及成本边界。
- **手机与可访问性**：730px 以下改为单栏，并用顶部吸附摘要保持 APR 可见。使用语义标签、错误关联、礼貌播报与键盘焦点提示；触摸模式放大滑块和折叠入口，减少动态效果偏好会关闭过渡与动画。
- **Active 读取控件与状态**：可选查询区沿用白色面板、蓝色主按钮和带标签的输入；RPC 设置默认折叠。读取时按钮显示忙碌文字并禁用，反馈在控件下方用文字与颜色共同表达状态，保留手动填写入口。更换查询条件或重新读取会清除旧 Active；成功反馈附区块与时间，失效或不支持状态不沿用旧结果。730px 以下查询控件纵向排列，主按钮占满一行。
