# LP 边际年化计算器

<!-- impeccable:product-schema 1 -->

## Platform

web

## 用户与目的

用户已有集中流动性仓位，希望在相同价格区间、相同资金分布下，比较追加不同金额会给整个仓位多带来多少手续费收入。

## 功能与口径

- 输入现有本金（U）、当前 active 份额（%）、现有仓位手续费 APR（%）、追加本金（U）。
- active 指当前活跃流动性中自己入场后的份额，不是资金利用率，也不是全池 TVL 份额。
- 核心结果为整笔新增本金的边际手续费年化；同时给出追加后份额、每天与每小时新增手续费。
- 可通过追加金额滑块和曲线比较；区分整笔追加的平均边际与下一小笔资金的瞬时边际。
- 假设区间、布仓分布、其他 LP、总手续费规模不变。输入与输出均为单利 APR，未计入币价盈亏、IL、gas、调仓成本，不是净收益预测。
- 2000 U、70%、60% APR、追加 1000 U 是解释用示例，不是实时池子数据。
- 可手填 Active，或用单个仓位详情链接 / NFT 编号读取。支持 BSC PancakeSwap v3、Uniswap v3/v4，Robinhood Chain Uniswap v3/v4。
- 同一区块计算仓位 L / 池当前活跃 L，明确区间下界包含、上界不包含；出区间、空仓位及自定义 Hook 等不支持状态不自动代入年化。
- 用户明确要求纯前端。年化计算在浏览器完成，无钱包、账号或交易。链上读取直接访问公共主备 RPC，用户可以用仅当前页面有效的自定义 HTTPS RPC 覆盖；不默认保存地址，不上传本金和 APR。

## 实现选择

原生 HTML/CSS/JavaScript；viem 用于合约编解码，esbuild 将依赖打包到本地资源。Node 仅用于本地静态服务、测试和打包。GitHub Pages 托管，无自建后端或数据库。独立目录，不修改相邻 LP 项目。

## 成功标准

打开首屏即可编辑四项输入、读到新增本金的 APR；错误输入不继续展示旧结果；移动端与键盘可用；计算可用独立测试重复验证。
