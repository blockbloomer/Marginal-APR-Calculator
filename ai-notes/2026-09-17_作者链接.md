# 作者链接

- 用户要求两个公开计算器都增加作者的推特链接。
- 在现有页脚增加「作者：青蛙 · @BlockBloomer」，跳转至 `https://x.com/BlockBloomer`；使用新标签页并设置 `noopener noreferrer`。
- 沿用现有蓝色链接与键盘聚焦样式，提供 44px 点击高度，桌面与手机都显示。
- 不引入依赖，不调整计算逻辑、参数、链上查询或隐私说明。
- 发布沿用 `main` 推送触发的 GitHub Pages 工作流。
- 本地验证：30 项现有测试、JavaScript 语法检查、静态构建和 `git diff --check` 均通过。
